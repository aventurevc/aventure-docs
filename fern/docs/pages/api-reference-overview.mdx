---
title: API reference
subtitle: Endpoints, parameters, and schemas in the public aVenture API
slug: api-reference
---

The reference is generated from the released public OpenAPI specification.
Endpoints are grouped by the domain they operate on —
entities, people, news, fundraising, media, search, and classifications.

## Before you start

- **[Authentication](/authentication)** — three credential types, and which
  endpoints each one reaches. Read the `security` block on an endpoint rather
  than assuming from its HTTP method: many `POST` endpoints are reads.
- **[Pagination and filtering](/pagination-and-filtering)** — use the parameters
  documented for each list endpoint.
- **[Errors](/errors)** — interpret API problem details.

## Base URL

```
https://api.aventure.vc
```

Use the path shown for each operation.

## OpenAPI document

The released specification is published unchanged, so it carries the release
version, every operation's `security` requirement, media types, and schema
constraints. Internal generation metadata is excluded from the published document.

<Download sources={["https://raw.githubusercontent.com/aventurevc/aventure-docs/fb8d0b8e5f9fcb70157dba08814d8f4641e35548/openapi/openapi.json"]} filename="openapi.json">Download openapi.json</Download>

The `info.version` field identifies the released document.

Fern also serves a rendering it regenerates from the same document at
[/openapi.json](/openapi.json). That rendering powers the API Explorer, but it
changes version, authentication, media types, some status codes, examples, and
schema composition. Use the Download for code generation and contract validation.

## Types for TypeScript

Request and response types are published as
[`@aventurevc/api-schemas`](https://www.npmjs.com/package/@aventurevc/api-schemas):

```bash
npm install @aventurevc/api-schemas
```

It ships schemas and types only — there is no generated client, so use `fetch`
or any HTTP library you prefer.
