---
title: aVenture Docs
slug: welcome
hide-page-actions: true
hide-nav-links: true
hide-feedback: true
layout: overview
subtitle: Start downloading private company data within minutes with the aVenture API, CLI, MCP server and SDK.
---

<div className="welcome-hero-tint">
  <img src="/docs/assets/welcome-light.jpg" className="block dark:hidden" style={{pointerEvents:'none',cursor:'default',transform:'none',transition:'none',animation:'none'}} />
  <img src="/docs/assets/welcome-dark.jpg" className="hidden dark:block" style={{pointerEvents:'none',cursor:'default',transform:'none',transition:'none',animation:'none'}} />
</div>

## Get started

<CardGroup cols={3}>
  <Card title="aVenture API" icon="duotone plug">
    Access private company data programmatically with the aVenture REST API
  </Card>

  <Card title="aVenture CLI" icon="duotone terminal">
    Download and manage private company data directly from your terminal
  </Card>

  <Card title="aVenture MCP" icon="duotone robot">
    Integrate private company data into AI workflows with the aVenture MCP server
  </Card>
</CardGroup>

## What you can query

<CardGroup cols={3}>
  <Card title="Entities" icon="duotone building">
    Companies, investors, and accelerators, filterable by industry, stage, location, funding, and accelerator cohort
  </Card>
  <Card title="People" icon="duotone user">
    Founders and investors, their roles at entities, and their investment activity
  </Card>
  <Card title="Fundraising" icon="duotone chart-line-up">
    Rounds, amounts raised, valuations, and investor participation
  </Card>
  <Card title="News" icon="duotone newspaper">
    Articles linked to the entities and people they mention
  </Card>
  <Card title="Search" icon="duotone magnifying-glass">
    Keyword, semantic, and cross-domain search over entities, people, and news
  </Card>
  <Card title="Classifications" icon="duotone tags">
    The canonical industry, product, model, and ownership taxonomies behind the filters
  </Card>
</CardGroup>

## Your first request

<Steps>
<Step title="Get a credential">

The API accepts three credential types, and which one you hold decides what you
can do. Read [Authentication](/authentication) to pick the right one.

</Step>
<Step title="Make a request">

Every call is a plain HTTPS request against `https://api.aventure.vc`:

```bash
curl -H "X-Client-Secret: $AVENTURE_CLIENT_SECRET" \
  "https://api.aventure.vc/v1/entities?size=5"
```

The [quickstart](/quickstart) walks through the first few calls in full.

</Step>
<Step title="Narrow the results">

`GET /v1/entities` alone accepts more than sixty filters. [Pagination and
filtering](/pagination-and-filtering) covers how to combine them and page through
the results.

</Step>
</Steps>

## Working in TypeScript

Request and response types are published as [`@aventurevc/api-schemas`](https://www.npmjs.com/package/@aventurevc/api-schemas)
on npm. It ships schemas and types only — bring your own HTTP layer.

```bash
npm install @aventurevc/api-schemas
```
